<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.restheart</groupId>
    <artifactId>restheart-parent</artifactId>
    <version>9.9.0-SNAPSHOT</version>
  </parent>

  <properties>
    <native.gc>--gc=serial</native.gc>
    <native.march>-march=compatibility</native.march>
    <mongodb.port>27017</mongodb.port>
    <!-- Which integration tests failsafe runs. The default matches every IT class, which is
         what the previous (unconfigured) failsafe default did — all 33 IT classes in this
         module end in 'IT'. Override to run a subset, e.g. only the Karate suite:
             mvn verify -Pmongodb -Dit.includes=**/RunnerIT.java -->
    <it.includes>**/*IT.java</it.includes>
    <!-- Whether the integration-test phases start/stop the MongoDB container and the RESTHeart
         server. Defaults to skipITs, i.e. they run whenever the ITs run. Set to true to reuse a
         MongoDB and a RESTHeart that are already running, so the tests can be re-run without
         rebuilding — see CONTRIBUTING.md. -->
    <skipTestEnv>${skipITs}</skipTestEnv>
  </properties>

  <groupId>org.restheart</groupId>
  <artifactId>restheart</artifactId>
  <packaging>jar</packaging>

  <name>restheart</name>
  <description>RESTHeart Core - Core services for RESTHeart</description>
  <url>https://restheart.org</url>
  <inceptionYear>2014</inceptionYear>

  <licenses>
    <license>
      <name>GNU Affero General Public License (AGPL) version 3.0</name>
      <url>http://www.gnu.org/licenses/agpl-3.0.html</url>
      <distribution>repo</distribution>
      <comments>Core components license</comments>
    </license>
    <license>
      <name>RESTHeart COMMERCIAL LICENSE</name>
      <url>https://github.com/SoftInstigate/restheart/blob/master/COMM-LICENSE.txt</url>
      <distribution>manual</distribution>
      <comments>Optional commercial license</comments>
    </license>
  </licenses>

  <dependencies>
    <dependency>
      <groupId>org.restheart</groupId>
      <artifactId>restheart-commons</artifactId>
    </dependency>
    <dependency>
      <groupId>io.github.classgraph</groupId>
      <artifactId>classgraph</artifactId>
    </dependency>
    <dependency>
      <groupId>info.picocli</groupId>
      <artifactId>picocli</artifactId>
    </dependency>
    <dependency>
      <groupId>org.graalvm.sdk</groupId>
      <artifactId>nativeimage</artifactId>
    </dependency>
    <!-- truffle-api on main classpath so system classloader can find libtruffleattach -->
    <dependency>
      <groupId>org.graalvm.truffle</groupId>
      <artifactId>truffle-api</artifactId>
      <version>${truffle.version}</version>
    </dependency>
    <!-- BEGIN Test dependencies -->
    <dependency>
      <groupId>org.restheart</groupId>
      <artifactId>restheart-mongodb</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.restheart</groupId>
      <artifactId>restheart-accounts</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.restheart</groupId>
      <artifactId>restheart-stripe</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.restheart</groupId>
      <artifactId>restheart-security</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.restheart</groupId>
      <artifactId>restheart-test-plugins</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.intuit.karate</groupId>
      <artifactId>karate-junit5</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.httpcomponents</groupId>
      <artifactId>fluent-hc</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.eclipsesource.minimal-json</groupId>
      <artifactId>minimal-json</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.konghq</groupId>
      <artifactId>unirest-java</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.graalvm.js</groupId> <!-- this fixes karate test hanging due to conflict in GraalVM releases -->
      <artifactId>js-language</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- END Test dependencies -->
  </dependencies>

  <build>
    <finalName>${project.artifactId}</finalName>
    <extensions>
      <extension>
        <groupId>org.springframework.build</groupId>
        <artifactId>aws-maven</artifactId>
      </extension>
    </extensions>
    <testResources>
      <testResource>
        <directory>src/test/java</directory>
        <excludes>
          <exclude>**/*.java</exclude>
        </excludes>
      </testResource>
      <testResource>
        <directory>src/test/resources</directory>
      </testResource>
    </testResources>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>license-maven-plugin</artifactId>
        <configuration>
          <verbose>true</verbose>
          <includes>
            <includes>**/*.java</includes>
          </includes>
        </configuration>
        <executions>
          <execution>
            <id>generate-license-headers</id>
            <goals>
              <goal>update-file-header</goal>
            </goals>
            <phase>process-sources</phase>
            <configuration>
              <licenseName>agpl_v3</licenseName>
              <addJavaLicenseAfterPackage>false</addJavaLicenseAfterPackage>
              <emptyLineAfterHeader>true</emptyLineAfterHeader>
              <skipUpdateLicense>${skipUpdateLicense}</skipUpdateLicense>
              <processStartTag>========================LICENSE_START=================================</processStartTag>
              <processEndTag>=========================LICENSE_END==================================</processEndTag>
              <roots>
                <root>src/main/java</root>
                <root>src/test</root>
              </roots>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <phase>prepare-package</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/lib</outputDirectory>
              <includeScope>runtime</includeScope>
              <excludeArtifactIds>restheart,restheart-security,restheart-mongodb,restheart-graphql,restheart-mongoclient-provider,restheart-polyglot,restheart-metrics</excludeArtifactIds>
            </configuration>
          </execution>
          <!-- unpack truffleattach resources -->
          <execution>
            <id>unpack-truffleattach</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>unpack-dependencies</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/lib/truffleattach</outputDirectory>
              <includes>META-INF/resources/engine/libtruffleattach/**</includes>
              <includeScope>runtime</includeScope>
              <includeArtifactIds>truffle-api</includeArtifactIds>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <configuration>
          <archive>
            <manifest>
              <mainClass>org.restheart.Bootstrapper</mainClass>
              <addClasspath>true</addClasspath>
              <classpathPrefix>lib/</classpathPrefix>
              <useUniqueVersions>false</useUniqueVersions>
              <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
              <addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
            </manifest>
            <manifestEntries>
              <Build-Time>${maven.build.timestamp}</Build-Time>
              <Enable-Native-Access>ALL-UNNAMED</Enable-Native-Access>
            </manifestEntries>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <configuration>
          <includes>
            <include>${it.includes}</include>
          </includes>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>io.fabric8</groupId>
        <artifactId>docker-maven-plugin</artifactId>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>dist</id>
            <phase>package</phase>
            <configuration>
              <target>
                <ant antfile="./copy-plugins.xml" target="copy" />
              </target>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <executions>
          <execution>
            <id>with-deps</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <finalName>restheart</finalName>
              <appendAssemblyId>false</appendAssemblyId>
              <descriptors>
                <descriptor>assembly.xml</descriptor>
              </descriptors>
              <attach>false</attach>
              <tarLongFileMode>posix</tarLongFileMode>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.sonatype.central</groupId>
        <artifactId>central-publishing-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <profile>
      <id>shade</id>
      <dependencies>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-security</artifactId>
        </dependency>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-mongodb</artifactId>
        </dependency>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-polyglot</artifactId>
        </dependency>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-graphql</artifactId>
        </dependency>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-mongoclient-provider</artifactId>
        </dependency>
      </dependencies>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-shade-plugin</artifactId>
            <configuration>
              <finalName>restheart-shaded</finalName>
              <createDependencyReducedPom>true</createDependencyReducedPom>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/MANIFEST.MF</exclude>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                    <exclude>META-INF/versions/9/module-info.*</exclude>
                    <exclude>module-info.class</exclude>
                    <exclude>LICENSE</exclude>
                    <exclude>LICENSE.md</exclude>
                    <exclude>LICENSE-ClassGraph.txt</exclude>
                    <exclude>pipes-fork-server-default-log4j2.xml</exclude>
                  </excludes>
                </filter>
                <filter>
                  <artifact>org.restheart:restheart</artifact>
                  <excludes>
                    <exclude>org/restheart/security/authorizers/package-info.class</exclude>
                    <exclude>org/restheart/utils/package-info.class</exclude>
                  </excludes>
                </filter>
                <filter>
                  <artifact>org.mongodb:bson</artifact>
                  <excludes>
                    <exclude>META-INF/native-image/org.mongodb/bson/native-image.properties</exclude>
                  </excludes>
                </filter>
                <filter>
                  <artifact>com.graphql-java:graphql-java</artifact>
                  <excludes>
                    <exclude>META-INF/maven/com.google.guava/guava/pom.properties</exclude>
                    <exclude>META-INF/maven/com.google.guava/guava/pom.xml</exclude>
                    <exclude>META-INF/proguard/*</exclude>
                  </excludes>
                </filter>
              </filters>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer" />
                <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheLicenseResourceTransformer" />
                <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheNoticeResourceTransformer">
                  <addHeader>false</addHeader>
                </transformer>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer" />
                <transformer implementation="org.apache.maven.plugins.shade.resource.IncludeResourceTransformer">
                  <resource>META-INF/RESTHEART-LICENSE.txt</resource>
                  <file>../LICENSE.txt</file>
                </transformer>
                <transformer implementation="org.apache.maven.plugins.shade.resource.IncludeResourceTransformer">
                  <resource>META-INF/RESTHEART-COMM-LICENSE.txt</resource>
                  <file>../COMM-LICENSE.txt</file>
                </transformer>
              </transformers>
            </configuration>
            <executions>
              <execution>
                <phase>package</phase>
                <goals>
                  <goal>shade</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-assembly-plugin</artifactId>
            <executions>
              <execution>
                <id>shaded</id>
                <phase>package</phase>
                <goals>
                  <goal>single</goal>
                </goals>
                <configuration>
                  <finalName>restheart</finalName>
                  <appendAssemblyId>true</appendAssemblyId>
                  <descriptors>
                    <descriptor>assembly-shaded.xml</descriptor>
                  </descriptors>
                  <attach>false</attach>
                  <tarLongFileMode>posix</tarLongFileMode>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <!--
        This profile is used to run the integration tests against a MongoDB docker contaniner.
        If you are already running MongoDB locally, disable this profile:
        mvn -P-mongodb clean verify
    -->
    <profile>
      <id>mongodb</id>
      <activation>
        <property>
          <name>!skipTests</name>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>io.fabric8</groupId>
            <artifactId>docker-maven-plugin</artifactId>
            <configuration>
              <skip>${skipTestEnv}</skip>
              <showLogs>false</showLogs>
              <removeVolumes>true</removeVolumes>
              <executeStopOnVMShutdown>true</executeStopOnVMShutdown>
              <images>
                <image>
                  <!--
                      mongodb/mongodb-atlas-local bundles mongod + mongot as a
                      self-initializing single-node replica set, so restheart-ai's
                      $vectorSearch/createSearchIndexes-dependent karate tests
                      (karate/ai/*.feature) work out of the box. Deliberately NO <cmd>
                      here: this image's own ENTRYPOINT does the replica-set init and
                      mongot wiring — a custom command (the old bind-ip-all/replSet
                      flags, needed for the plain mongo:X image) overrides that
                      entrypoint and breaks the image.
                  -->
                  <name>${mongodb.image}:${mongodb.version}</name>
                  <run>
                    <env>
                      <DO_NOT_TRACK>1</DO_NOT_TRACK>
                    </env>
                    <ports>
                      <port>${mongodb.port}:${mongodb.port}</port>
                    </ports>
                    <!-- remove showLogs=false above to get logs -->
                    <log>
                      <prefix>[mongodb]</prefix>
                      <enabled>true</enabled>
                      <color>yellow</color>
                    </log>
                    <wait>
                      <tcp>
                        <mode>mapped</mode>
                        <host>localhost</host>
                        <ports>
                          <port>${mongodb.port}</port>
                        </ports>
                      </tcp>
                      <!-- longer than the plain-mongo default: this image also brings
                           up mongot and self-initializes the replica set before it's
                           actually ready to serve, not just accept TCP connections -->
                      <time>30000</time>
                      <shutdown>1000</shutdown>
                    </wait>
                  </run>
                </image>
              </images>
            </configuration>
            <executions>
              <execution>
                <id>start</id>
                <phase>pre-integration-test</phase>
                <goals>
                  <goal>start</goal>
                </goals>
              </execution>
              <execution>
                <id>stop</id>
                <phase>post-integration-test</phase>
                <goals>
                  <goal>stop</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <!--
        Alternative to the "mongodb" profile above: starts the OFFICIAL mongo image
        (needs its own bind-ip-all/replSet command flags plus a separate rs.initiate()
        step in build.xml — it does not self-initialize like mongodb-atlas-local does)
        instead of mongodb/mongodb-atlas-local. Used by CI's compatibility matrix to
        test against specific official MongoDB releases (including ones with no
        mongodb-atlas-local equivalent tag, e.g. 8.3) — those legs get no
        $vectorSearch/createSearchIndexes support, so karate/ai/*.feature must be
        skipped there too (see RunnerIT's karate.vectorSearch system property).

        Never active by default — must be explicitly selected, deactivating the default
        "mongodb" profile in the same invocation:
            mvn clean verify -P-mongodb,mongodb-classic \
                -Dmongodb.image=mongo -Dmongodb.version=8.3 -Dkarate.vectorSearch=false
    -->
    <profile>
      <id>mongodb-classic</id>
      <build>
        <plugins>
          <plugin>
            <groupId>io.fabric8</groupId>
            <artifactId>docker-maven-plugin</artifactId>
            <configuration>
              <skip>${skipTestEnv}</skip>
              <showLogs>false</showLogs>
              <removeVolumes>true</removeVolumes>
              <executeStopOnVMShutdown>true</executeStopOnVMShutdown>
              <images>
                <image>
                  <name>${mongodb.image}:${mongodb.version}</name>
                  <run>
                    <cmd>--bind_ip_all --replSet rs0</cmd>
                    <ports>
                      <port>${mongodb.port}:${mongodb.port}</port>
                    </ports>
                    <log>
                      <prefix>[mongodb]</prefix>
                      <enabled>true</enabled>
                      <color>yellow</color>
                    </log>
                    <wait>
                      <tcp>
                        <mode>mapped</mode>
                        <host>localhost</host>
                        <ports>
                          <port>${mongodb.port}</port>
                        </ports>
                      </tcp>
                      <time>10000</time>
                      <shutdown>1000</shutdown>
                    </wait>
                  </run>
                </image>
              </images>
            </configuration>
            <executions>
              <execution>
                <id>start</id>
                <phase>pre-integration-test</phase>
                <goals>
                  <goal>start</goal>
                </goals>
              </execution>
              <execution>
                <id>stop</id>
                <phase>post-integration-test</phase>
                <goals>
                  <goal>stop</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <!--
        This profile:
         * initializes the MongoDB cluster using the mongo shell.
         * starts the restheart server before the integration tests
         * stops the restheart server after the integration tests
    -->
    <profile>
      <id>start-server</id>
      <activation>
        <property>
          <name>!skipTests</name>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <executions>
              <execution>
                <id>start-server</id>
                <phase>pre-integration-test</phase>
                <configuration>
                  <skip>${skipTestEnv}</skip>
                  <target>
                    <ant antfile="./build.xml" target="start-server" />
                  </target>
                </configuration>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
              <execution>
                <id>stop-server</id>
                <phase>post-integration-test</phase>
                <configuration>
                  <skip>${skipTestEnv}</skip>
                  <target>
                    <delete file="./target/plugins/restheart-test-plugins.jar" />
                    <echo level="info">***** Stopping RESTHeart process...</echo>
                    <exec dir="${basedir}" executable="bin/stop.sh">
                    </exec>
                  </target>
                </configuration>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>native</id>
      <dependencies>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-security</artifactId>
        </dependency>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-mongodb</artifactId>
        </dependency>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-polyglot</artifactId>
        </dependency>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-graphql</artifactId>
        </dependency>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-mongoclient-provider</artifactId>
        </dependency>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-metrics</artifactId>
        </dependency>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-accounts</artifactId>
        </dependency>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-emails</artifactId>
        </dependency>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-stripe</artifactId>
        </dependency>
        <dependency>
          <groupId>org.graalvm.js</groupId> <!-- required to override test dependency declared to fix karate test hanging due to conflict in GraalVM releases -->
          <artifactId>js-language</artifactId>
          <scope>runtime</scope>
        </dependency>
        <!-- uncomment to add test plugins to the native image -->
        <!-- <dependency>
         <groupId>org.restheart</groupId>
         <artifactId>restheart-test-plugins</artifactId>
          <version>${project.version}</version>
        </dependency> -->
      </dependencies>
      <build>
        <plugins>
          <plugin>
            <groupId>org.graalvm.buildtools</groupId>
            <artifactId>native-maven-plugin</artifactId>
            <!-- See https://graalvm.github.io/native-build-tools/latest/maven-plugin.html#configuration-options -->
            <configuration>
              <imageName>restheart</imageName>
              <buildArgs>
                <buildArg>${native.gc}</buildArg>
                <buildArg>${native.march}</buildArg>
                <buildArg>-J-Xmx10g</buildArg>
              </buildArgs>
            </configuration>
            <extensions>true</extensions>
            <executions>
              <execution>
                <id>build-native</id>
                <goals>
                  <goal>compile-no-fork</goal>
                </goals>
                <phase>package</phase>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <!-- activate with: mvn ... -Pnative,test-native -->
    <profile>
      <id>test-native</id>
      <dependencies>
        <dependency>
          <groupId>org.restheart</groupId>
          <artifactId>restheart-test-plugins</artifactId>
          <version>${project.version}</version>
        </dependency>
      </dependencies>
    </profile>
  </profiles>
</project>