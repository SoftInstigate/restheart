RESTHeart has been deployed.

{{- if .Values.ingress.enabled }}

Access the API via:
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}

{{- else }}

Access the API by running:

  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "restheart.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8080:{{ .Values.containerPort }}

Then visit: http://127.0.0.1:8080

{{- end }}

Health check: curl http://127.0.0.1:8080/ping

Run tests: helm test {{ .Release.Name }}

For configuration reference, see chart/RESTHEART-CONFIG.md or https://restheart.org/docs/setup/#configuration-files
